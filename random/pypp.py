import pyinputplus as pyip  #The as pyip code in the import statement saves us from typing pyinputplus each time we want to call a PyInputPlus function.
response = pyip.inputStr("Enter: ") # or use (prompt="Enter")
