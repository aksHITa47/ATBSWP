1. What is a relative path relative to?
   
   Relative to the current working directory.


2. What does an absolute path start with?
   
   It begins with the Drive(in windows), anchor or the root


3. What does Path('C:/Users') / 'Al' evaluate to on Windows?
   
   C:\Users\Sl


4. What does 'C:/Users' / 'Al' evaluate to on Windows?

   Error as '/' operator cannot be used on 'str' data types. A Path object is must if we wanna join paths using '/'.


5. What do the os.getcwd() and os.chdir() functions do?
   
   os.getcwd() returns the path of the current working directory \ os.chdir(path) changes the current working directory to path


6. What are the . and .. folders?
   
   . refers the present directory and .. refers to the parent folder.


7. In C:\bacon\eggs\spam.txt, which part is the dir name, and which part is the base name?
   
   dir name is:  C:\bacon\eggs and base name is: \spam.txt


8. What are the three “mode” arguments that can be passed to the open() function?
   
   'read','write' and 'append'.


9. What happens if an existing file is opened in write mode?
   
   The existing text gets overwritten by the new text.


10. What is the difference between the read() and readlines() methods?
     
    read() returns the content of the file as a single string, whereas the readlines() returns the content of the files as list of string breaking at the newline character in the file


11. What data structure does a shelf value resemble?
    
    dictionary